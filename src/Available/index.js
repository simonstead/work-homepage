import React from "react";
import styles from './styles.css'

export const Available = () => (
  <div className={styles.Available}>
    <h1>
      I <span className={styles.Am}>am</span> currently available for new work
    </h1>
  </div>
);

export default Available;
